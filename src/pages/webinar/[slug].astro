---
import matter from '../../lib/matter.js';
import BaseLayout from '../../layouts/BaseLayout.astro';
import AllPost from '../../islands/AllPost.jsx';
import { getSeoForPath } from '../../lib/fetchSeoData.js';

export async function getStaticPaths() {
  const files = import.meta.glob('../../../content/allPosts/webinar__*.mdx', { as: 'raw', eager: true });
  return Object.entries(files).map(([filePath, raw]) => {
    const fileName = filePath.split('/').pop();
    const slug = fileName.replace(/^webinar__/, '').replace(/\.mdx$/, '');
    const { data, content } = matter(raw);
    const latestPosts = Object.entries(files)
      .filter(([fp]) => fp !== filePath)
      .map(([, r]) => {
        try {
          const { data: d } = matter(r);
          return {
            slug: d.slug || r.split('/').pop().replace(/^webinar__/, '').replace(/\.mdx$/, ''),
            pageType: 'webinar',
            title: d.title || '',
            image: d.image || null,
            date: d.date ? String(d.date) : null,
          };
        } catch { return null; }
      })
      .filter(Boolean)
      .sort((a, b) => (b.date || '').localeCompare(a.date || ''))
      .slice(0, 5);

    return {
      params: { slug },
      props: {
        postData: {
          slug: data.slug || slug,
          pageType: data.pageType || 'webinar',
          pagetype: data.pagetype || 'Webinar',
          title: data.title || '',
          image: data.image || null,
          date: data.date ? String(data.date) : null,
          body: content.trim(),
        },
        latestPosts,
      },
    };
  });
}

const { slug } = Astro.params;
const { postData, latestPosts } = Astro.props;
const seo = getSeoForPath(`/webinar/${slug}`);

const pageProps = {
  data: { allPosts: postData },
  sorteddata: latestPosts,
};
---
<BaseLayout seo={seo} path={`/webinar/${slug}`}>
  <AllPost client:load {...pageProps} />
</BaseLayout>
